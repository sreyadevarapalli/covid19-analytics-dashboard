import SectionTitle from "../ui/SectionTitle";

function CountryHeader() {
  return (
    <SectionTitle
      title="Countries"
      subtitle="Explore COVID-19 statistics for every country."
    />
  );
}

export default CountryHeader;